{
  "name": "prooftrail",
  "version": "0.8.2",
  "description": "Prooftrail — checks your AI coding agent against the rules you set, at the end of every turn. Set your rules once (tests ran, they passed, the run happened after the last edit, and more), and every session decides the mechanical ones locally and instantly, with no extra model call. Pair a hosted judge to also check the rules that need judgment, and whether the final message is supported by what actually happened. Advisory: it never blocks.",
  "author": {
    "name": "Jason (Domain Methods)"
  },
  "userConfig": {
    "SERVICE_URL": {
      "type": "string",
      "title": "Review service URL",
      "description": "Origin of the review API. Defaults to the hosted service; override for self-hosted deployments.",
      "default": "https://supervisor-api.jason-8e5.workers.dev"
    },
    "LOCAL_AGENT": {
      "type": "string",
      "title": "Local agent for deep claim checks (optional)",
      "description": "OFF unless set. `auto` uses whichever of codex or agy is installed, or name one. When a final message claims something the session trace cannot support, Prooftrail asks that CLI to verify it against the actual repository, read-only and in the background, and reports the answer on the next turn. It runs on YOUR model credentials and nothing leaves your machine.",
      "required": false,
      "default": ""
    },
    "API_TOKEN": {
      "type": "string",
      "title": "Device token",
      "description": "Paired device token. Leave blank on the host (run /prooftrail:setup instead); set this only for sandbox surfaces, whose plugin data is wiped per session.",
      "required": false,
      "sensitive": true
    }
  }
}
